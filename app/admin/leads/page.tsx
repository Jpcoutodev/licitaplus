import { PainelLeads } from "./painel-leads";

export const metadata = {
  title: "Leads",
  robots: { index: false, follow: false },
};

export default function PaginaLeads() {
  return <PainelLeads />;
}
